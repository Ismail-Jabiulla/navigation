import 'package:flutter/material.dart';

class f_Add extends StatelessWidget {
  const f_Add({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Accounts')),
    );
  }
}
